server:
  port: ${PORT:8443}
  tomcat:
    mime-types: text/html,text/xml,text/plain,text/css,text/javascript,application/javascript,application/json
    compression: on
  servlet:
    session:
      timeout: 180m

spring:
  jpa:
    show-sql: false  # Smanjuje SQL logove u konzoli i logovima
    properties:
      hibernate:
        format_sql: false
        use_sql_comments: false
        jdbc.batch_size: 50  # Poboljšava performanse za batch operacije
        order_inserts: true
        order_updates: true
    open-in-view: false
  datasource:
    url: jdbc:mysql://77.105.37.50:3306/automate_orders?useUnicode=true&characterEncoding=utf8&autoReconnect=true&useSSL=false&serverTimezone=Europe/Amsterdam
    username: automate
    password: hIOe8800
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      idle-timeout: 60000
      max-lifetime: 180000    # 3 min
      keepalive-time: 180000  # pinguje konekcije da ih održi živim
      connection-timeout: 60000
      validation-timeout: 3000
      data-source-properties:
        cachePrepStmts: true
        prepStmtCacheSize: 250
        prepStmtCacheSqlLimit: 2048
        useServerPrepStmts: true
        sessionVariables: wait_timeout=300,interactive_timeout=300


# Putanje za slike proizvoda
roba:
  slika:
    tdPrefix: "/home/ftpuser/automaterijal/misc/tecdoc/slike/"
    publicPrefix: "/images/roba/"
    noImageUrl: "/images/no-image/no-image.png"

blog:
  cover-image:
    directory: "/home/ftpuser/automaterijal/misc/blog/slike/"

logging:
  level:
    root: INFO
    org.hibernate: WARN
    org.hibernate.SQL: WARN
    org.hibernate.type: WARN

sitemap:
  blog-prefix: /blog
  products:
    refresh-cron: 0 0 2 * * *
  vehicles:
    prefix: /webshop/vozila
    linking-target-type: PO
    refresh-cron: 0 30 3 * * *
    model-end-cutoff-year: 1995

integration:
  febi:
    enabled: true
    client-id: clktr_e9b5a2c8302cda39719738de3f28cf7f
    client-secret: O1CgWaLoswnZHuI81R0gwzeI03DQTZw9
    price-list-path: ${FEBI_PRICE_LIST_PATH:/home/ftpuser/automaterijal/misc/providers/febi/febi-pricelist.xlsx}
    bypass-article-errors: true
    order:
      mode: LIVE
  szakal:
    enabled: true
    order:
      mode: LIVE
    data-dir: /home/ftpuser/automaterijal/misc/providers/szakal
    sync:
      enabled: true
